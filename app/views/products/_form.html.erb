<%= simple_form_for(@product, html: {class: 'form-horizontal' }) do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.association :category %>
  <%= f.association :brand %>
  <%= f.button :submit %>
<% end %>
